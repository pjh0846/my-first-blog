STEP 1. 로컬에서 Django 프로젝트 준비
1-1. 가상환경 생성 및 Django 설치
bashmkdir djangoenv
cd djangoenv
python -m venv ./venv

# Windows
.\venv\Scripts\activate.bat

# Mac/Linux
source ./venv/bin/activate

pip install django
1-2. 프로젝트 및 앱 생성
bashdjango-admin startproject mysite .
python manage.py startapp blog

STEP 2. 파일 수정
2-1. mysite/settings.py 수정
pythonimport os

TIME_ZONE = 'Asia/Seoul'
LANGUAGE_CODE = 'ko'

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'static')

ALLOWED_HOSTS = ['127.0.0.1', '.pythonanywhere.com']

INSTALLED_APPS = [
    ...
    'blog',   # 추가
]
2-2. blog/models.py 작성
pythonfrom django.conf import settings
from django.db import models
from django.utils import timezone

class Post(models.Model):
    author = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE
    )
    title = models.CharField(max_length=200)
    text = models.TextField()
    created_date = models.DateTimeField(default=timezone.now)
    published_date = models.DateTimeField(blank=True, null=True)

    def publish(self):
        self.published_date = timezone.now()
        self.save()

    def __str__(self):
        return self.title
2-3. blog/admin.py 수정
pythonfrom django.contrib import admin
from .models import Post

admin.site.register(Post)
2-4. mysite/urls.py 수정
pythonfrom django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('blog.urls')),
]
2-5. blog/urls.py 새로 생성
pythonfrom django.urls import path
from . import views

urlpatterns = [
    path('', views.post_list, name='post_list'),
]
2-6. blog/views.py 수정
pythonfrom django.shortcuts import render

def post_list(request):
    return render(request, 'blog/post_list.html', {})
2-7. 템플릿 폴더 구조 생성
djangoenv/
└── blog/
    └── templates/
        └── blog/
            └── post_list.html   ← 이 파일 생성
2-8. blog/templates/blog/post_list.html 작성 (45페이지 내용)
html<html>
  <head>
    <title>KHU's blog</title>
  </head>
  <body>
    <div>
      <h1><a href="">KHU's blog</a></h1>
    </div>

    <div>
      <p>published: 14.06.2014, 12:14</p>
      <h2><a href="">My first post</a></h2>
      <p>Aenean eu leo quam. Pellentesque ornare sem lacinia quam 
      venenatis vestibulum. Donec id elit non mi porta gravida at 
      eget metus.</p>
    </div>

    <div>
      <p>published: 14.06.2014, 12:14</p>
      <h2><a href="">My second post</a></h2>
      <p>Aenean eu leo quam. Pellentesque ornare sem lacinia quam 
      venenatis vestibulum. Donec id elit non mi porta gravida at 
      eget metus.</p>
    </div>
  </body>
</html>

STEP 3. DB 생성 및 로컬 서버 확인
bashpython manage.py makemigrations blog
python manage.py migrate
python manage.py createsuperuser   # admin 계정 생성
python manage.py runserver
브라우저에서 http://127.0.0.1:8000/ 접속해서 화면 확인 ✅

STEP 4. GitHub에 코드 올리기
4-1. .gitignore 파일 생성 (djangoenv 폴더 안에)
*.pyc
*~
__pycache__
venv
db.sqlite3
/static
.DS_Store
4-2. Git 초기화 및 push
bashgit init
git config --global user.name "Your Name"
git config --global user.email you@example.com

git add --all .
git commit -m "My Django Blog, first commit"
4-3. GitHub에서 새 저장소 생성

github.com 접속 → New repository → 이름: my-first-blog → Create

4-4. 원격 저장소 연결 및 push
bashgit remote add origin https://github.com/[내아이디]/my-first-blog.git
git push -u origin master

STEP 5. PythonAnywhere 배포
5-1. 회원가입

www.pythonanywhere.com → Beginner(무료) 계정 생성
아이디가 홍길동이면 → URL: 홍길동.pythonanywhere.com

5-2. Bash 콘솔에서 코드 가져오기
bashgit clone https://github.com/[내아이디]/my-first-blog.git

cd my-first-blog
virtualenv --python=python3.10 venv
source venv/bin/activate
pip install django
5-3. DB 생성
bashpython manage.py migrate
python manage.py createsuperuser
5-4. Web App 설정

PythonAnywhere 대시보드 → Web 탭
Add a new web app 클릭
도메인 확인 → Manual configuration 선택 (Django 아님!)
Python 3.10 선택

5-5. 가상환경 경로 입력
Web 탭 → Virtualenv 섹션에 입력:
/home/[내아이디]/my-first-blog/venv
5-6. WSGI 파일 수정
Web 탭 → WSGI configuration file 클릭 → 내용 전체 삭제 후 아래 입력:
pythonimport os
import sys

path = '/home/[내아이디]/my-first-blog'
if path not in sys.path:
    sys.path.append(path)

os.environ['DJANGO_SETTINGS_MODULE'] = 'mysite.settings'

from django.core.wsgi import get_wsgi_application
from django.contrib.staticfiles.handlers import StaticFilesHandler
application = StaticFilesHandler(get_wsgi_application())
저장 후 → Reload 버튼 클릭